# Python (mcp/)
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/

# Generated by running backmeup.configure.sh/install.sh directly against a
# checkout (it writes into bin/ in place before install.sh copies it out) -
# never meant to be committed here, only in an actual install directory.
/bin/backmeup.setup.sh
/bin/backmeup.setup.sh.old
